{% set name = "mechkit" %}
{% set version = "0.0.4" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: "cf5721b9eb2e90a4ade548ab81a5cd3bed8633ef3cf616a8b276ad3afb7242e2"

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv "

requirements:
  host:
    - numpy
    - pip
    - python
  run:
    - numpy
    - python

test:
  imports:
    - mechkit

about:
  home: "https://github.com/JulianBauerKIT/mechkit"
  license: "MIT"
  license_family: "MIT"
  license_file: ""
  summary: "Basic tools for continuum mechanics using numpy"
  doc_url: ""
  dev_url: ""

extra:
  recipe-maintainers:
    - your-github-id-here
